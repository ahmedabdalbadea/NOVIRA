import 'package:flutter/material.dart';

const kBackgroundGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [
    Color(0xFFF3E5F5), // Soft Lavender/Purple (Top)
    Color(0xFFE1F5FE), // Light Blue (Bottom)
  ],
);

const kPrimaryColor = Colors.white70;
const kButtonGradient = LinearGradient(
  colors: [
    Color(0xFF5CB6F2), // Light Blue
    Color(0xFF8DE09C), // Light Green
  ],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);
